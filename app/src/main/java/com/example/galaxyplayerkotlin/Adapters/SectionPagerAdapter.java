/*
package com.example.com.example.galaxyplayerkotlin.Adapters;

import LoginFragment;
import PlayFragment;
import SongListFragment;

import androidx.annotation.Nullable;
import androidx.fragment.app.Fragment;
import androidx.fragment.app.FragmentManager;
import androidx.fragment.app.FragmentPagerAdapter;

import static MainActivityClass.LOGIN_FRAGMENT;
import static MainActivityClass.PLAY_FRAGMENT;
import static MainActivityClass.SONG_LIST_FRAGMENT;

public class SectionPagerAdapter extends FragmentPagerAdapter {

    public SectionPagerAdapter(FragmentManager fm) {
        super(fm);
    }

    @Override
    public Fragment getItem(int position) {

        switch (position) {
            case SONG_LIST_FRAGMENT:
                return new SongListFragment();
            case PLAY_FRAGMENT:
                return new PlayFragment();
            case LOGIN_FRAGMENT:
                return new LoginFragment();
            default:
                return null;

        }

    }

    @Override
    public int getCount() {
        return 2;
    }

}
*/
